Beekeeper::Api
==============

Installation
------------

Add this line to your application's Gemfile:

```ruby
gem 'beekeeper-api'
```

And then execute:

    $ bundle

Or install it yourself as:

    $ gem install beekeeper-api

Usage examples
--------------
